upstream partythemegenerator {
    server service_1:5000;
}

server {
    listen   80;
    server_name localhost;

    location / {
        proxy_read_timeout 180;
        proxy_pass http://partythemegenerator;
    }
}

